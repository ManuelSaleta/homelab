- Management:
    - Proxmox:
        icon: proxmox
        href: https://192.168.50.200:8006
        description: Proxmox Virtual Environment
        widget:
          type: proxmox
          url: https://192.168.50.200:8006
          username: "homepage-widgets-api@pve!homepage-proxmox-widgets"
          password: "{{HOMEPAGE_VAR_PROXMOX_WIDGET_PASSWORD}}"
          node: mothership
          show:
            - vms
- Infrastructure:
    - Cloudflare-Pod:
        icon: cloudflare.png
        href: https://one.dash.cloudflare.com/
        description: Cloudflare Tunnel Daemon
        widget:
          type: kubernetes
          namespace: networking
          podSelector: app=cloudflared
- Network:
    - Cloudflare-Tunnel:
        icon: cloudflare.png
        href: https://one.dash.cloudflare.com/
        widget:
          type: cloudflared
          accountid: "{{HOMEPAGE_VAR_CF_ACCOUNT_ID}}"
          tunnelid: "{{HOMEPAGE_VAR_CF_TUNNEL_ID}}"
          key: "{{HOMEPAGE_VAR_CF_API_TOKEN}}"
          fields:
            - status
            - origin_ip
    - micro-nas:
        icon: tailscale.png
        href: https://login.tailscale.com/admin/machines
        widget:
          type: tailscale
          deviceid: "{{HOMEPAGE_VAR_TAILSCALE_NAS_DEVICE_ID}}"
          key: "{{HOMEPAGE_VAR_TAILSCALE_API_KEY}}"
          label: "NAS Status"
          fields: [address, last_seen]
    - macbook-pro:
        icon: tailscale.png
        href: https://login.tailscale.com/admin/machines
        widget:
          type: tailscale
          deviceid: "{{HOMEPAGE_VAR_TAILSCALE_LAPTOP_DEVICE_ID}}"
          key: "{{HOMEPAGE_VAR_TAILSCALE_API_KEY}}"
          label: "Laptop Status"
          fields: [address, last_seen]
# Monitoring stack (kube-prometheus-stack, Grafana) currently uninstalled
# - Monitoring:
#     - Grafana:
#         icon: grafana.png
#         href: http://promstack-grafana.monitoring.svc.cluster.local:80
#         widget:
#           type: grafana
#           version: 2
#           url: http://promstack-grafana.monitoring.svc.cluster.local:80
#           username: "{{HOMEPAGE_VAR_GRAFANA_USERNAME}}"
#           password: "{{HOMEPAGE_VAR_GRAFANA_PASSWORD}}"
#           show_dashboards: true
#           show_datasources: true
#           show_alerts: true

